# Deprecated VPS configuration. Firebase is the only supported production target.
# Do not copy this file into a production host. Use Firebase Secret Manager and
# inject the release variables through the reviewed Firebase deployment scripts.

VITE_FIREBASE_API_KEY=
VITE_GA_MEASUREMENT_ID=
JWT_SECRET=
MIGRATION_SECRET=
